import styled from 'styled-components';

const ReviewRowWrapper = styled.section`
  display: flex;
  border-top-color: rgb(231, 231, 231);
  border-top-style: solid;
  border-top-width: 1px;
`;

export default ReviewRowWrapper;
